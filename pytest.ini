[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: full OCR + YOLO per image (exclude in quick CI with -m "not slow")
    needs_weights: requires runs/train_id_detectr_hyper/weights/best.pt
